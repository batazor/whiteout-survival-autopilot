name: Train Infantry

node: infantry

priority: 10

trigger: troops.infantry.state.isAvailable

ttl: 2h

steps:
  - if:
      trigger: troops.infantry.state.textStatus == "completed"
      then:
        - click: troops_get_button
        - wait: 500ms

  - if:
      trigger: troops.infantry.state.textStatus == "idle"
      then:
        - click: troops_get_button
        - wait: 500ms
        - click: troops_train_start_button
