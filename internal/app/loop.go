package app

// (Optional) If you want a continuous loop in a goroutine, you could do so here.
// For example:

// func (a *App) mainLoop(...) error {
//     // while true: evaluate triggers, run usecases, etc.
//     // But we might do that inside the TUI instead.
//     return nil
// }
